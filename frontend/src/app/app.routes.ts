import { Routes } from '@angular/router';
import { GovDataComponent } from './components/gov-data/gov-data.component';

export const routes: Routes = [
  { path: '', component: GovDataComponent }, // Default route
];
